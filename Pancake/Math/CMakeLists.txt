PROJECT(PANCAKE_MATH)

FILE(GLOB HEADERS *.h)
FILE(GLOB SOURCES *.cpp)

SET(${PROJECT_NAME}_HEADER ${HEADERS} CACHE INTERNAL "")
SET(${PROJECT_NAME}_SOURCE ${SOURCES} CACHE INTERNAL "")

ADD_LIBRARY(${PROJECT_NAME} ${SOURCES})